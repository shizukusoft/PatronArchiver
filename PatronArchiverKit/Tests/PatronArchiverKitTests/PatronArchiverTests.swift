import Testing
@testable import PatronArchiverKit

/// Suites for each part of ``PatronArchiver`` live in `PatronArchiverTests+*.swift`, mirroring the
/// extension files they cover.
struct PatronArchiverTests {}
